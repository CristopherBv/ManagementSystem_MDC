package gestionmats.dao;
import gestionmats.models.Pedido;

public interface IPedidoDAO extends IDAO<Pedido> {
    // Aquí podrías agregar métodos como: List<Pedido> findByStatus(EstadoPedido status);
}